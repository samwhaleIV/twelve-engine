namespace TwelveEngine {
    public enum Impulse:int {
        /* Don't change the order of the key binds enum.
         * A strict order allows for backwards compatibility */
        Up, Down, Left, Right, Accept, Cancel, Add, Subtract
    }
}
