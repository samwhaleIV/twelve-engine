namespace ElfGame.ElfSprite.Face {
    internal enum EyeBruising { None, Left, Right, All }
    internal enum EyeTears { None, Left, Right, All }
    internal enum EyeType { Normal, Dazed, Sly, Uncaring, None }
    internal enum EyeBlink { None, LeftWink, RightWink, Blink }
    internal enum MouthType { Normal, Scream, Sad, None }
    internal enum MouthMode { Idle, Talk }
}
